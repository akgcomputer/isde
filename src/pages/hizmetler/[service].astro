---
// src/pages/hizmetler/[service].astro
import RedirectTemplate from '../../components/RedirectTemplate.astro';

export const prerender = false; // İstek anında dinamik çalışması için SSR aktif
---

<RedirectTemplate />
