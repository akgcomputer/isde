---
// src/pages/user/orders.astro
import DashboardLayout from '../../layouts/DashboardLayout.astro';

export const prerender = false;

const user = Astro.locals.user;
if (!user || user.is_partner !== 0) {
  return Astro.redirect('/login?error=unauthorized');
}

// TODO: Gerçek siparişleri veritabanından çekilecek
const mockOrders = [
  { id: 'SİP-10042', status: 'Hazırlanıyor', date: '2026-06-10', total: '1.250 TL', items: 'Web Sitesi Kurulumu' },
  { id: 'SİP-10038', status: 'Tamamlandı', date: '2026-05-15', total: '750 TL', items: 'Aylık SEO Paketi' }
];
---

<DashboardLayout title="Siparişlerim | İş De Yeter" activeTab="orders">
  <div class="max-w-6xl mx-auto space-y-6">
    <div class="flex items-center justify-between">
      <div>
        <h1 class="text-2xl font-bold text-gray-900">Siparişlerim</h1>
        <p class="text-gray-500 mt-1">İş De Yeter üzerinden verdiğiniz hizmet siparişlerinin durumu.</p>
      </div>
    </div>

    <div class="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
      {mockOrders.length > 0 ? (
        <div class="overflow-x-auto">
          <table class="w-full text-left border-collapse">
            <thead>
              <tr class="bg-gray-50 border-b border-gray-200 text-sm text-gray-600">
                <th class="py-4 px-6 font-semibold">Sipariş Kodu</th>
                <th class="py-4 px-6 font-semibold">İçerik</th>
                <th class="py-4 px-6 font-semibold">Tarih</th>
                <th class="py-4 px-6 font-semibold">Tutar</th>
                <th class="py-4 px-6 font-semibold">Durum</th>
                <th class="py-4 px-6 font-semibold">İşlem</th>
              </tr>
            </thead>
            <tbody class="divide-y divide-gray-200 text-sm">
              {mockOrders.map(order => (
                <tr class="hover:bg-gray-50 transition-colors">
                  <td class="py-4 px-6 font-medium text-gray-900">{order.id}</td>
                  <td class="py-4 px-6 text-gray-600">{order.items}</td>
                  <td class="py-4 px-6 text-gray-600">{order.date}</td>
                  <td class="py-4 px-6 font-semibold text-gray-900">{order.total}</td>
                  <td class="py-4 px-6">
                    <span class={`inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium ${order.status === 'Tamamlandı' ? 'bg-green-100 text-green-800' : 'bg-blue-100 text-blue-800'}`}>
                      {order.status}
                    </span>
                  </td>
                  <td class="py-4 px-6">
                    <button class="text-blue-600 hover:text-blue-800 font-medium text-xs">Detaylar</button>
                  </td>
                </tr>
              ))}
            </tbody>
          </table>
        </div>
      ) : (
        <div class="p-12 text-center text-gray-500">
          <i class="fa-solid fa-box-open text-4xl mb-4 text-gray-300"></i>
          <p>Henüz bir siparişiniz bulunmamaktadır.</p>
        </div>
      )}
    </div>
  </div>
</DashboardLayout>
